workflow:
  rules:
    - if: '$CI_COMMIT_REF_PROTECTED == "true" && $CI_COMMIT_TAG =~ /(^[0-9]$|^[0-9A-Za-z][0-9A-Za-z._-]{0,33}[0-9]$)/'
    - when: never

stages:
  - github-sync
  - gitlab-release
  - gitlab-cleanup

include:
  - project: '${GL_CICD_SHARED_PROJ}'
    ref: main
    file: '/github/validate.yml'
  - project: '${GL_CICD_SHARED_PROJ}'
    ref: main
    file: '/github/push.yml'
  - project: '${GL_CICD_SHARED_PROJ}'
    ref: main
    file: '/gitlab/validate.yml'
  - project: '${GL_CICD_SHARED_PROJ}'
    ref: main
    file: '/gitlab/release.yml'
  - project: '${GL_CICD_SHARED_PROJ}'
    ref: main
    file: '/dbus/release-build.yml'
  - project: '${GL_CICD_SHARED_PROJ}'
    ref: main
    file: '/gitlab/cleanup.yml'

variables:
  GL_CICD_SHARED_PROJ: 'core-assets/cicd/delivery'
  GH_RELEASE_PUSH: "false"
  GH_FORCE_PUSH: "false"
  GL_RELEASE_ASSET: "true"
  GL_CLEAR_GLAB: "false"
  APPEND_VERSION: "false"
  APPLY_PATCHES: "true"
  APPLY_PATCHES_EXCLUDE: ""
  PATCH_RELEASE_DIR: "patches/release"
  PATCH_SERIES_FILE: "patches/release/series"
  GIT_BRANCH_PREFIX: "mcr"
  GIT_BRANCH_RELEASE: "release"
  # dbus-broker needs a real Rust-capable runner image; the shared helper only fills gaps.
  GL_CICD_RUNNER_BUILD: '${GL_CICD_RUNNER_RUST}'
  GL_CICD_RUNNER_RELEASE: '${GL_CICD_RUNNER_UBUNTU}'
